// const useGenre=(genre)=>{
//     if(genre.length<1) return "";


//     const selectedGenreId = genre.map(g=>g.id)
//    return selectedGenreId.reduce((acc,curr)=> acc + "," + curr)
  
// }
// export default useGenre


export const hello ="hello"